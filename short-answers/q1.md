# Question 1

What is the primary difference between an HTTP GET request and an HTTP Post request?

# Answer
The primary difference between an HTTP `GET` request and an HTTP `POST` request is that `GET` fetches and retrieves data *from* a server while `POST` sends and submits data *to* the server.

# Teacher Feedback